import React from 'react';

const Taxonomy = () => {
    return (
        <div>
            <h2>Taxonomy</h2>
            <p>This section includes a taxonomy related to DNS over HTTPS.</p>
        </div>
    );
};

export default Taxonomy;
